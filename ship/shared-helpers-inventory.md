# Shared helpers inventory — LIMS Control Lab

> **Why this file exists — lesson L-Duplication.** Vertical-slice building produces horizontal
> duplication: a fresh session per vertical can't see what an earlier session already wrote, so it
> re-derives the helper instead of importing it. Consult this table before writing any new utility,
> formula, or shared component — this domain is explicitly calculation-heavy (calibration chains,
> weighted averages, tolerance rules recurring across many analysis types and sites), which is exactly
> the shape of logic that gets silently re-derived per vertical if the canonical implementation isn't
> visible (`engineering-standards.md` §6).
>
> **How it's used:**
> - **`/ship:build`** — check this table *before* writing any new utility/formula/component; import/extend
>   an existing row instead of writing a new one. Add a row here in the same change that introduces a
>   genuinely new shared helper.
> - **`/ship:verify`**'s dedup dimension checks new code against this inventory.

## Helpers

| Helper | File | Use for | Added |
|--------|------|---------|-------|
| `CalculationEngine` | `backend/src/LimsControlLab.Domain/Calculations/CalculationEngine.cs` | All calibration-curve lookups, weighted averages, and composite-result derivations — the single source for every calculation in brief R39–R41; no analysis-specific formula is re-derived inline in a service | populated by Task 6 (`InterpolateCalibrationValue` — DI-free, direct-unit-tested linear interpolation) — genuinely wired into `AnalysisExecutionService.CaptureReadingAsync`'s lock-gated recompute (verified directly, not just via test count, after the first submission's recompute path turned out to be an empty placeholder) |
| `IAuditLogger` | `backend/src/LimsControlLab.Domain/Auditing/IAuditLogger.cs` (impl: `backend/src/LimsControlLab.Infrastructure/AuditLogger.cs`) | Every consequential write (status change, unlock, exception decision) calls this once to record user/role/timestamp/before-after — never a bespoke audit insert per service | pre-registered 2026-08-25 — **populated by Task 3**, verified working (real `AuditLogs` rows on status change + exception decision) |
| `ICurrentUser` | `backend/src/LimsControlLab.Domain/Auth/ICurrentUser.cs` | The single source for "who is calling" (id, role, site claims) — every authorization check and audit-log call reads from this, never a second parallel claims lookup | pre-registered 2026-08-25 (shape v0.1.0) — populated by plan Task 2 |
| `TimeProvider` (injected, not `DateTime.UtcNow`) | DI-registered in `Api/Program.cs` | Every timestamp (audit entries, shift/schedule calculations, lock timestamps) — swappable and testable, never a direct clock call in a service | pre-registered 2026-08-25 (shape v0.1.0) — actually populated by Task 2 (`JwtTokenService` is its first consumer), not Task 1 as originally pre-registered |
| `IUnitOfWork` | `backend/src/LimsControlLab.Infrastructure/IUnitOfWork.cs` | Any write touching more than one aggregate (e.g. an exception decision that also changes analysis lock state) — wraps the EF execution strategy + transaction, never two independent `SaveChangesAsync` calls | pre-registered 2026-08-25 — interface scaffolded Task 2; **note for Task 4+: not yet a real caller** — Task 3's status-change/exception-decision writes persist only because `IAuditLogger.LogAsync` happens to call `SaveChangesAsync` on the same scoped `DbContext` afterward, not via `IUnitOfWork` as originally intended. Worth tightening when a task first needs a genuine multi-aggregate transaction. |
| `OutcomeExtensions` (`Outcome<T>` → `ActionResult`/`ProblemDetails`) | `backend/src/LimsControlLab.Api/Common/OutcomeExtensions.cs` | Every controller action's final line — maps `Ok/NotFound/Invalid/Forbidden/Conflict` to the right HTTP status + RFC-7807 body (Conflict includes the current `rowVersion`) — never hand-rolled per action | **new** — populated by Task 3 (shape.md §1 requires this shared mapping; the first submission skipped it and was sent back) |
| `PagedResult<T>` + `PagingExtensions.ToPagedResultAsync` | `backend/src/LimsControlLab.Api/Common/{PagedResult,PagingExtensions}.cs` | Every server-paged list endpoint (search/history, template list, schedule list) — one page-size-clamp implementation (max 500), never reimplemented per controller | scaffolded Task 3, genuinely consumed by Task 8's `SearchController` — **caller's query must stay a real EF-translatable `IQueryable` all the way in**; materializing to a `List`/`IEnumerable` before calling this breaks `.CountAsync()` (confirmed the hard way on Task 8, see plan.md) |
| Paged-list signal helper + `EmptyState` component | `frontend/src/app/shared/{paged-list.ts,empty-state.ts}` | Every Kendo Grid screen's page-state handling and every empty-list render — never hand-rolled per feature | pre-registered 2026-08-25 (shape v0.1.0) — populated by plan Task 15/17 |
| `BaseApiService` + `LimsApiService` (abstract API base classes, extracted from the real `Databank.WebApp`/`BaseApiService`+`DatabankApiService` pattern) | `frontend/src/app/shared/services/api/{base-api.service.ts,lims/lims-api.service.ts}` | Every feature's concrete API service extends `LimsApiService`; reads via `httpResource`/`rxResource`, mutations via the inherited `post`/`put`/`delete` — no raw `HttpClient`, `fetch`, or `Promise`-returning API calls anywhere | pre-registered 2026-08-25 (shape v0.1.0, corrected same day against `Databank.WebApp`) — populated by plan Task 15 |
| Generated API client (`ng-openapi-gen`, models + function-based services) | `frontend/src/app/shared/generated/` (regenerate via `npm run generate:api` against a running backend's `/openapi/v1.json`; excluded from ESLint as generated code, never hand-edited) | Every feature's concrete API service wraps the generated function for its endpoint(s) rather than hand-typing request/response shapes — keeps the frontend contract mechanically in sync with the backend OpenAPI spec (`engineering-standards.md` §4) | populated by Task 15 (fix-up round) — requires the backend's `Microsoft.AspNetCore.OpenApi` wiring (`Program.cs`, added by the orchestrator directly at the same task) to already be live |
| `CurrentUserService` + `AuthApiService` (signal-based auth state) | `frontend/src/app/shared/services/auth/{current-user.service.ts,auth-api.service.ts}` | Single source for current user's token, role, site claims — signal-based for reactivity, never a second parallel auth lookup; stores JWT in `localStorage` with signal synchronization | populated by Task 16; corrected during the same task's review — the real `POST /api/v1/auth/login` response (`LoginResponseDto`: `token`/`userId`/`role`/`site`) hands back role/site/userId directly, so a client-side JWT-decode step (the first submission's approach, `jwt.utils.ts`) was unnecessary and has been removed; the first submission's hand-typed `LoginResponse` interface (`access_token`/`token_type`/`expires_in`) also didn't match the real backend shape at all — caught only by an actual live login call against the running API, not by any test |
| `authGuard` (route guard) | `frontend/src/app/shared/guards/auth.guard.ts` | Protects authenticated routes, redirects unauthenticated users to `/login` — the single source for access control on the frontend (server still re-checks all writes per `engineering-standards.md` E1–E3) | **new** — populated by Task 16, checks `CurrentUserService.isAuthenticated()` via `token()` signal |
| `AuthenticatedLayout` (shell component) | `frontend/src/app/shared/layouts/authenticated-layout/authenticated-layout.ts` | Default authenticated shell (nav showing current user + role + site, logout action, `<router-outlet>` for feature content) — mounted as the authenticated routes' parent layout per layout-first routing | **new** — populated by Task 16, uses Kendo UI button primitives, calls `AuthApiService.logout()` on logout |

<Add a row per shared helper as it's written or adopted going forward.>
