# Shape — `v0.1.0`

- **Release:** v0.1.0  ·  **Written:** 2026-08-25  ·  **Delivery surface:** backend (codebase)
- **Built from** *(pointers, commit-pinned):* `plan.md@fa5f2fe` + `design-handoff.md@f65e295` + `engineering-standards.md@66271df`

> **To build:** §1 is the skeleton you copy — do not re-invent contracts or layering per slice, copy them
> from here. §2 is the running record of every decision Construction made; when you hit something
> undecided, route it back to `/ship:shape`, which appends here — never guess, and never reopen Design.

## 1. Technical shape — the copy template

This is a **greenfield vertical build** (backend/codebase, no existing LIMS platform codebase available to
this project — confirmed at audit time), so the shape below is the full copy template for plan Task 3
(the reference vertical) and every backend task that follows it, not a modification pattern.

### Contracts

- **API base:** REST, JSON, URL-versioned — `/api/v1/...`. OpenAPI emitted from the running API
  (`engineering-standards.md` §4); the Angular client (plan Task 15) generates from it, never hand-mirrored.
- **Concurrency contract:** every entity exposed via the API carries a `rowVersion` field (the EF Core
  concurrency token, charter §2). Writes echo the `rowVersion` they read; a stale write returns
  **409 Conflict** with a body naming the current `rowVersion` so the client can reload rather than
  silently overwrite.
- **Error contract:** RFC-7807 `application/problem+json` for every failure — 400 validation (naming the
  expected range, actual value, and reason per brief R38), 403 forbidden, 404 not found, 409 conflict,
  500 safety net. Every response carries a `correlationId` (`engineering-standards.md` §5).
- **Reference vertical endpoints (plan Task 3):**
  - `GET /api/v1/analyses/{analysisId}` → `AnalysisDetailDto { id, sampleId, templateId, status, readings[], exceptions[], rowVersion }`
  - `POST /api/v1/analyses/{analysisId}/readings` — body `{ testId, value, unit, capturedAtUtc, instrumentId? }` → 201 `ReadingDto { id, testId, value, unit, capturedAtUtc, capturedBy, validationResult }`
  - `POST /api/v1/analyses/{analysisId}/exceptions/{exceptionId}/decision` — body `{ decision: "Modify" | "Retest" | "AcceptWithComment", comment, rowVersion }` (comment mandatory — brief R36) → 200 updated `ExceptionDto`
  - `PATCH /api/v1/analyses/{analysisId}/status` — body `{ action: "Start" | "Pause" | "Resume" | "Cancel", rowVersion }` → 200 updated status, or 409 on stale `rowVersion`
- **What must NOT change:** the `rowVersion` + 409 concurrency contract and the RFC-7807 error envelope are established here for every subsequent backend task (4–13) to reuse verbatim — a later task introducing a different error shape or skipping `rowVersion` on a concurrently-editable entity is a shape violation, not a valid local choice.

### Layering

`Controller → Service → Repository → DbContext` throughout (`engineering-standards.md`):

- **`AnalysesController`** (and every later controller) is thin: authorizes via `[Authorize]` policy, validates via the global FluentValidation-equivalent filter, delegates to its service, maps the returned `Outcome<T>` to the HTTP result via one shared extension method set. Controllers never return entities, only DTOs.
- **`AnalysisExecutionService`** (and every later service) holds the business logic, is HTTP-agnostic, returns `Outcome<T>` (`Ok / NotFound / Invalid / Forbidden / Conflict`). Calculation logic is never inlined here — it calls `CalculationEngine` (shared-helpers-inventory).
- **Repositories** (`IAnalysisRepository`, `ISampleRepository`, …) are the only types touching `LimsDbContext`. A write spanning more than one aggregate (e.g. an exception decision that also changes the analysis's lock state) goes through `IUnitOfWork`.
- **Cross-cutting services** — `ICurrentUser` (who's calling), `IAuditLogger` (writes the audit trail), `TimeProvider` (injected clock) — are constructor-injected into services, never instantiated ad hoc or read from `HttpContext` directly inside a service.
- **Angular:** one feature folder per vertical (`features/analysis-execution/`, …), standalone components throughout (no `Component` suffix / `.component.` infix — `analysis-execution.ts`, class `AnalysisExecution`), consistent `lims-` selector prefix. Each feature's API access goes through a concrete service extending `LimsApiService` (itself extending `BaseApiService`) — no raw `HttpClient`, no `fetch`, no `Promise`-returning API calls (per `engineering-standards.md`'s extracted `Databank.WebApp` convention). Reads prefer `httpResource`/`rxResource`; mutations use the `Observable`-returning `post`/`put`/`delete`. Smart/container components own state (signals) and API calls; presentational components stay pure.

### Shared helpers — resolved up front

Checked against `ship/shared-helpers-inventory.md` (created this run — first release, so every row is `new`, pre-registered so build reuses rather than re-derives across Tasks 2–15):

| Helper | Reuse / extend / new | Inventory row |
|--------|----------------------|---------------|
| `CalculationEngine` | new | `backend/src/LimsControlLab.Domain/Calculations/CalculationEngine.cs` |
| `IAuditLogger` | new | `backend/src/LimsControlLab.Domain/Auditing/IAuditLogger.cs` |
| `ICurrentUser` | new | `backend/src/LimsControlLab.Domain/Auth/ICurrentUser.cs` |
| `TimeProvider` (DI-registered) | new | `Api/Program.cs` registration |
| `IUnitOfWork` | new | `backend/src/LimsControlLab.Infrastructure/IUnitOfWork.cs` |
| `PagedResult<T>` + paging extension | new | `backend/src/LimsControlLab.Api/Common/PagedResult.cs` |
| Angular paged-list helper + `EmptyState` | new | `frontend/src/app/shared/{paged-list.ts,empty-state.ts}` |
| `BaseApiService` + `LimsApiService` (abstract API base classes) | new | `frontend/src/app/shared/services/api/{base-api.service.ts,lims/lims-api.service.ts}` |

## 2. Construction decision log

| # | Date | Type | Decision | Acceptance criteria (EARS) | Why | Routes to | Decided by |
|---|------|------|----------|----------------------------|-----|-----------|------------|
| C1 | 2026-08-25 | technical | API is URL-versioned (`/api/v1/...`), REST + JSON | n/a — structural | Neither brief nor charter names a versioning scheme; a structural call the requirements never needed to state | shape | shape |
| C2 | 2026-08-25 | technical | DTO naming: `<Entity>Dto` for detail, `<Entity>ListItemDto` for list rows; controllers never return entities | n/a — structural | `engineering-standards.md` implies this convention but doesn't spell out the exact naming; fixed once so every task follows the same pattern | shape | shape |
| C3 | 2026-08-25 | technical | Concurrency is mechanically implemented as a `rowVersion` field on every concurrently-editable DTO, echoed on writes, with 409 responses naming the current `rowVersion` | n/a — structural | Charter §2 decided *that* optimistic concurrency applies; it did not (and shouldn't have) specified the field name or 409 body shape — that's this stage's job | shape | shape |
| C4 | 2026-08-25 | technical | Audit logging is centralized in one `IAuditLogger` service called from each Service method after a successful write, never a bespoke audit insert per controller/service | n/a — structural | Brief R22/R23/R75 require a full audit trail but don't (and shouldn't) dictate the implementation pattern; centralizing it now prevents 13 independent audit-write implementations later (lesson L-Duplication) | shape | shape |
| C5 | 2026-08-25 | technical | Reference vertical's minimal entity set (plan Task 3) is `Sample`, `Analysis`, `AnalysisTemplate` (minimal fields only — full template configurability is plan Task 4), `ExceptionRecord` | n/a — structural | Scoping Task 3 tightly to the lifecycle+exception+audit+concurrency pattern (not full template richness) is what makes it a clean copy template rather than a partial slice of Task 4's work | shape | shape |

| C6 | 2026-08-25 | technical | Frontend conventions corrected to match the organisation's real `Databank.WebApp` codebase (Angular 21.1, Kendo v23, Vitest, `BaseApiService`/`httpResource` pattern, layout-first routing, four-tier environments) rather than generic Angular assumptions; `engineering-standards.md`, this file, `plan.md`, and `shared-helpers-inventory.md` updated accordingly | n/a — structural | The operator provided access to a real sibling Angular codebase (`C:\Users\j.bacalso\Documents\Databank\Databank.WebApp`) after the initial shape run; extracting its actual conventions is more reliable than an inferred default and corrects several specifics (test runner, API-layering pattern, naming) the first pass got only approximately right | shape | shape |

| C7 | 2026-08-25 | technical | No separate `Role` entity — `plan.md` Task 2's "`Domain/Entities/Role.cs`" row is superseded by Task 1's shared-kernel `Role` enum (C6). `User` carries `Role Role` and `Site Site` as direct enum properties (one role, one site per user, matching the "role and site claims" language in this file and `engineering-standards.md`); Task 2 creates only `Domain/Entities/User.cs`. | n/a — structural | Creating a second `Role` type after the canonical enum already exists would violate Task 1's own "exactly one canonical location" acceptance criterion | build | shape |
| C8 | 2026-08-25 | technical | Task 2 creates the initial `Infrastructure/LimsDbContext.cs` (minimal — a `Users` `DbSet` only) plus its own first EF Core migration, so auth has real persistence to authenticate against and its own tests are genuine (not stubbed). Task 3 **extends** that same context with `Samples`/`Analyses`/`AnalysisTemplates`/`ExceptionRecords` and adds its own migration — it does not create the context fresh, despite `plan.md`'s Task 3 file list reading "create ... `LimsDbContext.cs`" (read that row as "create the reference vertical's entities in the already-existing context", not "create the context"). | n/a — structural | `plan.md`'s dependency graph has Task 3 depend on Task 2; that only holds together if Task 2 is where the context and its first migration exist — otherwise Task 2 (depending on Task 1 alone) would have nothing to persist a `User` into, and the reference vertical's "reference" claim would rest on a context nobody reviewed as the copy template until Task 3 | build | shape |

| C9 | 2026-08-26 | technical | Task 16 (reference vertical, frontend) additionally builds the minimal shared authenticated shell layout (`shared/layouts/authenticated-layout/`) and a login screen (`features/auth/login/`) posting to the already-built `POST /api/v1/auth/login` — plus a functional route guard gating the authenticated layout's children on a valid token. `plan.md`'s Tasks 15-22 never lists an explicit login-screen task, but R73 (enterprise-standard authentication) and the backend's already-built JWT issuance (Task 2) make a login flow a structural precondition for reaching *any* protected screen. | n/a — structural | Mirrors the backend's Task-2-before-Task-3 pattern (auth built before the first protected vertical), for which the frontend backlog has no equivalent explicit task; Task 16 is where it has to land because every later feature (Tasks 17-20) depends on being able to reach a protected route at all — deferring it further would block every subsequent frontend task, not just this one | build | shape |

| C10 | 2026-08-26 | technical | R12's "work queue split into not-started/in-progress/completed" is served by `POST /api/v1/search/results` (Task 8) — already site-scoped to `_currentUser.Site` and returning each result's `Status`. No new "assigned to me" backend endpoint or Schedule/Analysis assignment link is built; the frontend calls Search with no extra filter and groups the returned `PagedResult<SearchResultItemDto>` rows into three columns by their existing `Status` field (`NotStarted`→not-started, `InProgress`/`OnHold`→in-progress, `Completed`→completed; `Cancelled` excluded from all three columns). | n/a — structural | Task 17's frontend session correctly refused to fabricate a client-side "my assignments" view since no per-analysis assignment concept exists anywhere in the data model (`Schedule.AssignedToUserId` is shift/analysis-type-level, not per-analysis) — routed back rather than guessing (exactly the right call). The real, decidable answer: R72's site-segregation already makes "site-scoped analyses" the practical meaning of "their work" in this domain, and Search already returns exactly that grouped-by-status data; inventing a separate endpoint would duplicate Search for no real behavioural gain. | build | shape |

| C11 | 2026-08-26 | technical | `GET /api/v1/samples/{sampleId}` is added (Task 20 needs it and none existed — `SampleTransferController` only had the `POST .../transfer` action). Visibility per R59: a caller may view the sample if their site matches `Sample.Site` (origin) **or** `Sample.CurrentSite` (current) — not a general `[Authorize]`-only pattern, since R59 requires both the transferring and receiving site to keep read access. | n/a — structural, directly derived from already-decided R59 text | Task 20's frontend session cannot build any transfer-visibility UI without a way to read a sample at all; the visibility rule is a direct, non-ambiguous reading of R59 ("transferring and receiving sites SHALL be able to view"), not a new product decision | build | shape |

| C12 | 2026-08-26 | technical | Task 21's automated a11y scan (AC: "an automated a11y scan of every built screen reports zero WCAG AA violations") uses `axe-core` added as a frontend devDependency, invoked directly inside Vitest component tests (jsdom) — one `*.a11y.spec.ts` per feature area (`analysis-execution`, `templates`, `scheduling`, `exception-review`, `history-search`, `sample-transfer`) plus the shared `authenticated-layout`/`login` shell, each rendering the component via `TestBed` and asserting `(await axe.run(fixture.nativeElement)).violations` is empty. The `color-contrast` rule is disabled in these runs specifically (jsdom has no real layout/paint engine, so contrast checks are meaningless there — a documented jsdom limitation, not a suppression of a real violation); every other WCAG 2.1 AA rule axe-core ships stays enabled. | n/a — structural | No a11y scanning tool exists anywhere in the frontend dependency tree yet; Task 22 (which adds Playwright for its E2E smoke suite) depends on Task 21 per `plan.md`'s dependency column, so Task 21 cannot wait on Task 22's not-yet-built Playwright infrastructure. `axe-core` in Vitest is the standard, dependency-light mechanism that works today, inside this project's existing Vitest+jsdom test runner, without waiting on sequencing it doesn't control. Task 22's later Playwright suite may optionally layer a real-browser `@axe-core/playwright` pass (catching color-contrast for real) as a bonus, not a Task 21 requirement. | shape | shape |

| C13 | 2026-08-26 | technical | **Corrects C6.** C6's frontend file-organisation claims were wrong — verified directly against `C:\Users\j.bacalso\Documents\Databank\Databank.WebApp\src\app` a second time, this time actually reading `features/*` and `shared/layouts/*` file listings rather than asserting from a generic Angular default. The real convention: (1) every feature/page component is **split into three files** — `<kebab-name>.component.ts` (class `<PascalName>Component`, e.g. `CalculationsComponent`), `<kebab-name>.component.html` (`templateUrl`), `<kebab-name>.component.scss` (`styleUrl`) — never inline `template:`/`styles:` strings; (2) layouts use `.layout.ts/.layout.html/.layout.scss` with a `<PascalName>Layout` class (no `Component` suffix), e.g. `default.layout.ts` → `DefaultLayout`; (3) **every feature folder owns its own `<kebab-name>.routes.ts`**, a plain exported `Routes` array using `loadComponent: () => import('./x.component').then(m => m.XComponent)` for lazy loading — not one shared monolithic routes file; (4) spec files stay unsuffixed (`<kebab-name>.spec.ts`, `<kebab-name>.a11y.spec.ts`) per the one real precedent found (`stops-import.spec.ts` beside `stops-import.component.ts`). The root `app.ts`/`app.html`/`app.config.ts`/`app.routes.ts` are correctly already un-suffixed in both codebases — no change needed there. **What C6 got right and stays unchanged:** standalone components (Databank's newer features are standalone; only its legacy layouts use `standalone: false`, not a pattern to copy), the `lims-` selector prefix, Vitest, the `BaseApiService`/`LimsApiService` pattern, four-tier environments. **Deliberately not copied:** Databank's legacy `@NgModule`-wrapped `RoutingModule` classes with nav/icon metadata (seen on `calculations.routes.ts`) — that serves Databank's drawer-nav system, which LIMS's simpler top-nav was never asked to replicate (charter/design-handoff named no such requirement); adopting it now would be an unrequested abstraction. LIMS's per-feature routes files export a plain `Routes` array, nothing more. **Separately, a real Kendo UI usage gap** (not a C6 error, a build-quality gap): `@progress/kendo-angular-dropdowns` was never installed, so `analysis-execution.ts`, `scheduling-form.ts`, and `templates-form.ts` all use native HTML `<select>` — a hand-rolled equivalent of a Kendo primitive, which `engineering-standards.md` forbids. `DateInputsModule` is imported in `analysis-execution.ts` but never actually used (no `<kendo-datepicker>` anywhere) — dead import over a plain text field. Both get fixed in the same remediation pass. | n/a — structural correction | The operator directly inspected the real `features/*` folders and asked why the built code didn't match, and separately flagged the missing Kendo usage — both confirmed true on inspection. C6 was recorded from an incomplete read of the reference repo; this correction is evidence-based, re-verified file-by-file this time, covering every built frontend task (15-20) plus the two in-flight Task 21 dispatches that were paused and discarded before they could compound the wrong shape further. | build | shape |

| C14 | 2026-08-26 | technical | `plan.md` Task 22's literal file list ("create: illustrative frontend fixture/demo data") is superseded: **no mock/demo data lives in the frontend at all.** The frontend only ever calls the real backend API; illustrative/mock data is backend-only, already established at Task 13 (`SeedData.cs`, gated `IsDevelopment()`). Task 22's seed-data half means *enriching* that existing backend seed data, not creating a second, separate frontend-side data source. Confirmed the frontend currently has zero hardcoded mock/demo data in its actual (non-spec) source (`grep -rln "mock" features/ shared/` outside `.spec.ts` returns nothing) — nothing to remove, just a rule to hold going forward. Additionally, `backend/SeedData.cs`'s illustrative analysis templates/products/methods should be sourced from the organisation's real reference workbook (`docs/Samples required from each site_Updated2024.xlsx` — confirmed present, e.g. its "Analysis Required"/"Method Analysis"/"Pan Products" sheets list real products (Sugar, A Massecuite), real analysis types (Pol, Brix, Water, Fibre, Sucrose, Dry Substance), and real methods (BSES, NIRA)) rather than arbitrary invented strings, wherever a real name is a straightforward drop-in replacement for a placeholder one — a modest, curated slice, not a full data-migration effort. | n/a — structural | Operator explicitly stated the mock-data policy (backend-only) and pointed at the reference workbook as a source for realistic seed values. Confirmed both parts (frontend already clean; workbook has real, directly-usable domain terms) before writing this row. | build | shape |

| C15 | 2026-08-26 | technical | **Real gap, not a new decision.** No code path anywhere in the backend ever set `Analysis.IsLocked = true`, and the `PATCH /api/v1/analyses/{id}/status` action switch (`AnalysisExecutionService.ChangeStatusAsync`) only ever produced `InProgress`/`OnHold`/`Cancelled` — never `Completed` (confirmed by grepping the entire `backend/src` tree: `IsLocked = true` appears nowhere; `"Complete"` is not a case in the action switch, despite line 299's `if (analysis.Status == LifecycleStatus.Completed)` dead-code guard already anticipating it). This meant Task 7's entire "unlock a locked result" feature and Task 9's Databank push (`if (!analysis.IsLocked || analysis.Status != LifecycleStatus.Completed)`) were unreachable in the real running system — both had passing tests only because those tests construct `Analysis { IsLocked = true, Status = Completed }` directly rather than driving it through the real API, the same "test doesn't exercise the real path" class of gap flagged before in this build, just load-bearing this time. Found while investigating why Task 22's E2E "review → unlock" loop couldn't be built at all — no analysis could ever reach a lockable state through any real workflow. **Fix (a direct, mechanical realisation of an already-decided rule, not a new product decision):** add `"Complete" => LifecycleStatus.Completed` to the action switch; when the transition lands on `Completed`, also set `IsLocked = true`, `LockedAtUtc`, `LockedByUserId` in the same write — this is exactly charter §2's already-decided "once locked, they freeze until a Lab Coordinator unlocks" rule, just actually wiring the "once locked" trigger that was missing. No new columns needed (`IsLocked`/`LockedAtUtc`/`LockedByUserId` already exist from Task 7). | n/a — structural, direct realisation of charter §2's already-decided lock-on-completion rule | Charter §2 already decided completed results become locked; this was never the requirements gap, it was a missed wiring — the "Complete" action and its lock side-effect were the two things Task 3/7 never actually connected. Not an addition: nothing new is being decided, an already-decided rule is being connected to reality. | build | shape |

| C16 | 2026-08-26 | technical | **Severe, real, previously-undetected gap.** There is no HTTP interceptor anywhere in the frontend, and no API service manually attaches an `Authorization` header either — confirmed by grepping the entire `frontend/src/app` tree for `Authorization`/`Bearer`/`Interceptor`, zero non-spec matches. `app.config.ts`'s `provideHttpClient()` registers no interceptors at all. This means every protected backend call the real running app makes is unauthenticated and gets a real `401` — confirmed live: a fresh navigation to the analysis-execution screen after a real login returns `401` on `GET /api/v1/analyses/1`, rendering as "Error Loading Analysis" (the exact screen an operator would see, not a test artifact). This was masked all build because most manual/live checks confirmed a screen *rendered* (nav bar, shell, login) without confirming its *protected content* loaded — the one place it was checked closely (this session's own Task 22 E2E work) is what surfaced it. **Fix:** add a standard functional `HttpInterceptorFn` (`shared/interceptors/auth.interceptor.ts`) reading the token from `CurrentUserService` and attaching `Authorization: Bearer <token>` to every outgoing request that has one, registered via `provideHttpClient(withInterceptors([authInterceptor]))` in `app.config.ts` — the standard, textbook Angular 15+ pattern; this is infrastructure a `BaseApiService`/`LimsApiService` foundation should have shipped with from Task 15, not a product decision. | n/a — structural, standard HTTP-client infrastructure for an already-decided JWT-bearer auth mechanism (charter §2) | Charter already decided bearer-token auth; wiring the interceptor that actually sends it is completing that decision, not making a new one. | build | shape |

| C17 | 2026-08-26 | technical | **Another real, previously-undetected contract mismatch, found the same way as C16.** `analysis-execution-api.service.ts` hand-writes its own `ExceptionDto` interface with fabricated `status: 'Open'|'Resolved'`, `analysisId`, and `comment` fields that don't exist on the real backend response (confirmed against the live API and backend source: the real shape is `id/readingId/reason/decision/rowVersion` only). `analysis-execution.component.html` does `exception.status.toLowerCase()`, which throws a real `TypeError` the instant any exception renders — confirmed live via a standalone Playwright script. Separately, `ExceptionRecord.DecisionComment` (a real, correctly-written entity field, R36's mandatory comment) was never exposed by the API at all — through two separate DTO layers (`ExceptionDto` and the service-layer `ExceptionDecisionResult`), both missing the field. **Fix:** frontend's `ExceptionDto` corrected to the real shape, template logic re-derived from `decision` presence (open = no decision, resolved = has one) instead of a fabricated `status` field; backend exposes `decisionComment` through both DTO layers so the frontend can actually display what was submitted. | n/a — structural, correcting an invented contract to match the real one | Nothing new is being decided — the real DTO shape and the real stored field already existed; this connects the frontend's assumption and the API's exposure to what's actually there. | build | shape |

| C18 | 2026-08-26 | addition | **UI library reversed (Kendo → Zard UI + Tailwind v4) + a full UI modernisation, operator-authorised (charter amendment A1, §3b).** Two things at once, both operator-decided, neither derivable from the original requirements. **(1) Library swap.** Every Kendo surface is replaced with the equivalent Zard UI primitive, generated into the repo via `npx zard-cli add` and owned under `shared/components/<name>/` (shadcn philosophy — not an npm-imported NgModule). Mapping (verified against real docs + real usage): `kendoButton`/`kendo-button`→Zard `button`; `kendoTextBox` + native inputs→Zard `input` (+ `textarea`); `kendo-datetimepicker`→Zard `date-picker` (native `datetime-local` where a time component is needed); `kendo-dropdownlist`→Zard `select`; `kendo-grid`/`kendo-grid-column` (exception-review, history-search, templates-list, scheduling-list)→Zard `table`; `kendo-dialog` (exception-review unlock)→Zard `dialog`. `provideZard()` is added to `app.config.ts`; `provideAnimationsAsync()` (added only because a Kendo component crashed without it) is removed **iff** a build+render check confirms nothing else needs it. The Kendo theme CSS import leaves `angular.json`; Tailwind v4 enters via `.postcssrc.json` (`@tailwindcss/postcss`) + `@import "tailwindcss"` in `src/styles.css`. All ~20 `@progress/kendo-*` packages, `@progress/kendo-licensing`, `@progress/kendo-drawing`, and the theme package leave `package.json`; `docs/telerik-license.txt` and plan.md §6's license-provisioning risk are removed. **(2) Modernisation.** A single Tailwind v4 design-system token layer (semantic colour/spacing/typography, light + dark) applied uniformly across the shell + all 7 feature areas; screens reworked to the BRD §6.11 UI/UX requirements (shift/day work split Not-started/In-progress/Completed per R12, rapid multi-reading entry, clear state + exception affordances, efficient sample→analysis→test navigation, desktop **and** tablet responsiveness) — the current build is "very simple" divs/labels; this makes it modern and easy to use for seasonal/rotating staff (HLD constraint). Realistic domain vocabulary for demo/empty states sourced from the reference workbook + BRD (sites PIONEER/INKERMAN/INVICTA/KALAMIA/VICTORIA/MACKNADE/PROSERPINE; products Sugar, A/B/C Massecuite & Molasses, Mud, ESJ, Bagasse; tests Pol/Brix/Water/RS/Ash/Fibre/Sucrose; methods BSES/NIRA; instruments Refractometer/Polarimeter/Mettler scales/HPLC). | (a) WHEN the frontend builds, the system SHALL contain zero `@progress/kendo-*` package deps and zero `kendo`/`@progress` references in non-generated source. (b) WHEN any screen renders an interactive control (button, text/number input, date-time, single-select, tabular list, modal), it SHALL use the corresponding Zard `shared/components/` primitive, never a hand-rolled or unstyled-native equivalent. (c) The system SHALL apply one cohesive Tailwind v4 token-based design system (light + dark) uniformly across the shell and every feature. (d) WHEN a Control Lab Analyst opens their landing screen, the system SHALL present their shift/day work split Not-started / In-progress / Completed with overdue + exception affordances (R12, BRD §6.11). (e) Every redesigned screen SHALL preserve the already-accepted behavioural contracts unchanged — rowVersion/409 threading, mandatory exception comment (R36), unlock justification (R46), R38 validation display, site-scoped visibility (R59) — redesign is visual/UX only. (f) Every redesigned screen SHALL still report zero WCAG AA violations (axe-core, C12) and the frontend test suite SHALL be green (specs updated from Kendo-DOM assertions to Zard equivalents). | Neither the library swap nor the modernisation is in `brief.md`/`charter.md` as originally confirmed — the charter confirmed Kendo (gate-log row 2) and the design-handoff froze a "very simple" UI. Both are **operator decisions taken after the confirmation gate**, so they are recorded as an `addition` naming the operator, and the library reversal is additionally a charter amendment (A1, §3b) because the constitution forbids stack substitution without one. **Definition/Design leak note:** the "make it modern and easy to use" intent maps directly to BRD §6.11, which existed at Definition — it could have shaped design-handoff had UX depth been pulled forward then rather than accepted as "very simple"; `/ship:retro` should read this as a Design-phase signal, not a build defect. | plan + build | Judison Bacalso (operator, orchestrator session) |

- **Additions this release:** 1  ·  **Technical decisions:** 17

C1–C17 are `technical` (shape-decided, reference-vertical era). **C18 is an `addition`** — the operator reversed the charter's UI-library decision (Kendo → Zard UI + Tailwind v4, charter amendment A1) and commissioned a full UI modernisation, neither derivable from the original requirements; recorded here with EARS criteria and named to the operator, routed to `plan` (redesign work to sequence) and `build`. **Can shape decide everything in front of it?** The technical shape of the replacement (component mapping, token layer, provider changes) is decided above; the operator's authorisation for the switch itself is recorded (A1). Cleared to `/ship:build`.
