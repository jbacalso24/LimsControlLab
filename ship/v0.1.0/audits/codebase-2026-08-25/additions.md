# What the design contains that nobody asked for — `v0.1.0` · codebase `2026-08-25`

Nothing was added, because nothing exists. This is a greenfield repository — there is no code beyond the `ship/` dossier and `docs/` source material, so there is nothing for this document to find.

## Needs a decision

*(empty — no rows)*

## The additions

*(none)*

## Divergences from the shared design system

*(none — no components exist)*

## Scope crossings

None. `brief.md` §7's out-of-scope list (external lab results, Factory Data, bioethanol equipment, etc.) has nothing to cross yet, since nothing has been built.

## Things that look like additions and are not

None to report — an empty repository cannot contain a false positive.
