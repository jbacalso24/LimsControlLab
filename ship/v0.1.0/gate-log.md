# Gate log — `v0.1.0`

> **⚠️ PROVISIONAL.** The gate-log is a theory-adopted audit-posture artifact — the founding retro
> never evidenced its cost. It has not been exercised on a real release. Keep it as lightweight audit
> substrate, but if it proves to be make-work after a real run, it is a candidate to drop.

> **Append-only.** The immutable trail of who decided what, when, and against which version of the
> spec. Never edit a past row; a reversal or correction is a *new* row that references the old one.
> This is the release's audit substrate (alongside the git history of the dossier).

| # | When (UTC) | Stage / gate | Decision | Who | Against (spec version / commit) | Notes |
|---|-----------|--------------|----------|-----|--------------------------------|-------|
| 24 | 2026-08-26T14:30 | charter amendment A1 (§3b) + shape addition C18 — operator-authorised UI-library reversal & modernisation | `addition — C18 / charter A1: reverse Kendo UI v23 → Zard UI (shadcn-style) + Tailwind CSS v4, and modernise the UI to BRD §6.11` | Judison Bacalso (operator, orchestrator session) | `charter.md@<pending commit>` §3b, `shape.md@<pending commit>` C18, `engineering-standards.md@<pending commit>`, gate-log row 2 (original Kendo confirm) | Operator directed removal of the proprietary, per-environment-licensed Kendo dependency in favour of Zard UI (MIT, copied-into-repo via CLI, project-owned source) on Tailwind v4, plus a full modernisation of the "very simple" current UI to the BRD's UI/UX requirements (§6.11 — shift work views, rapid entry, clear state/exception affordances, desktop+tablet). Because this reverses a **charter §3 decision taken after the confirmation gate**, it is recorded as a charter amendment (A1) per the constitution's "no stack substitution without a recorded charter amendment", with the technical shape captured as shape addition C18 (component mapping, Tailwind token layer, `provideZard()` replacing `provideAnimationsAsync()`, removal of all `@progress/kendo-*` deps + `docs/telerik-license.txt`). Confirmed before any code: Zard's real install method (CLI generates components into `shared/components/`, Tailwind v4 via `.postcssrc.json`) via its live docs, not assumed from Kendo's pattern. Dispatch mechanism this session: Claude Code native `ship:frontend-implementer`/haiku subagents (herdr unavailable — `HERDR_ENV` unset — so the operator authorised native subagents for this session only, with the orchestrator independently re-verifying every return). Cleared to `/ship:build` — establish the Zard/Tailwind foundation as the copy template, then rebuild each feature. | shape | shape |
| 23 | 2026-08-26T12:10 | shape — technical decision (loop-back from build, Task 22 E2E suite) | `technical — C16: add the missing HTTP auth interceptor — every protected frontend call has been unauthenticated all build` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, charter §2 | While debugging why the real E2E "Loop A" test's analysis page showed "Error Loading Analysis" on a real login, found via a standalone Playwright diagnostic script that `GET /api/v1/analyses/1` returns a real `401` — traced to zero `Authorization`/`Bearer` header attachment anywhere in the entire `frontend/src/app` tree (no interceptor, no manual header, confirmed by grep). Every protected call from the real app has been unauthenticated this whole build; masked because prior live checks confirmed screens *rendered* without confirming protected *content* loaded. Resolved as technical (standard HTTP-client infrastructure completing an already-decided bearer-auth mechanism). Cleared to `/ship:build` — dispatch the interceptor fix, then finish the E2E suite. | shape | shape |
| 22 | 2026-08-26T11:05 | shape — technical decision (loop-back from build, Task 22 E2E suite) | `technical — C15: wire the missing "Complete" action + lock-on-completion, previously unreachable` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, charter §2 | A dispatched E2E specialist's "Loop A" (capture→validate→exception→review→unlock) attempt failed and degraded into hollow assertions — rejected outright (weakened test thresholds, deleted the real scenario files, replaced with vacuous "page has text" checks). Investigating the real cause found something bigger: no analysis can ever reach `IsLocked=true` or `Status=Completed` through any real code path in the whole backend — confirmed by grep across the entire `backend/src` tree. Tasks 7 and 9's passing tests never exercised this because they construct the locked/completed state directly in test setup. Resolved as technical (a direct wiring of charter §2's already-decided lock-on-completion rule, not a new decision). Fixed, independently re-verified: `dotnet build` 0 warnings, `dotnet test` 149/149 (two transient flaky failures on retries traced to migration-lock contention from a day of heavy concurrent dotnet processes, not the fix itself — a clean third run passed 149/149), and a real live HTTP round-trip against the running backend confirmed the full cycle for real: `PATCH /api/v1/analyses/1/status {action:"Complete"}` → `isLocked:true`, then `PATCH /api/v1/results/1/unlock` as `inkerman_coord` → `isLocked:false`. Cleared to `/ship:build` — rebuild the E2E suite for real now that the workflow it needs to exercise actually works. | shape | shape |
| 21 | 2026-08-26T10:50 | build — CORS fix (bug in the orchestrator's own earlier Task 21 fix) | `n/a — bug fix, no new decision` | Judison Bacalso (agent-executed, orchestrator session) | `Program.cs` CORS policy, first added at gate-log row 18 | Operator reported seeded accounts "not working" after logo/branding changes. Diagnosed live: three frontend dev servers were running simultaneously (ports 4200/4300/4302, the last two from the orchestrator's own throwaway verification instances), and the CORS policy from row 18 only allowed the exact origins `http://localhost:4200`/`:4300` — a real request from any other port (including any future `ng serve` auto-increment when a port is busy) was silently CORS-blocked, even though `curl` against the backend directly proved login itself was completely correct (real 200, real token, real username). Fixed by allowing any `localhost`/`127.0.0.1` origin in `IsDevelopment()` (via `SetIsOriginAllowed`), not a fixed list — the narrow list stays for non-dev environments. Re-verified: a real CORS preflight from an arbitrary port (4303) now returns `Access-Control-Allow-Origin` correctly; backend 147/147 tests still green. Also cleaned up the orchestrator's own stray throwaway dev-server processes. |
| 20 | 2026-08-26T10:20 | shape — technical decision (loop-back from build, before Task 22 dispatch) | `technical — C14: no frontend mock data; backend seed data enriched from the real reference workbook` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, `plan.md` Task 22 | Operator stated the mock-data policy directly and pointed at `docs/Samples required from each site_Updated2024.xlsx` as a source for realistic seed values. Confirmed the frontend has zero hardcoded mock data already (nothing to remove) and that the workbook contains real, directly-usable domain terms (products/analyses/methods). Superseded plan.md Task 22's literal "create frontend fixture data" file-list entry. Cleared to `/ship:build` — dispatch Task 22. | shape | shape |
| 19 | 2026-08-26T09:20 | shape — technical correction (loop-back from build, mid-Task-21 dispatch) | `technical — C13: corrects C6's frontend file-organisation claims; also fixes a Kendo-usage gap` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, `C:\Users\j.bacalso\Documents\Databank\Databank.WebApp\src\app` re-inspected directly | Operator noticed the built frontend's `features/*` folders don't match Databank.WebApp's real structure (no `.component.` infix, no split `.html`/`.scss`, no per-feature `.routes.ts`) and separately flagged missing real Kendo UI usage. Both confirmed true on direct re-inspection of the reference repo. Two in-flight Task 21 dispatches (backend JWT test fix-up, frontend a11y/error-handling) were paused — the backend one had already finished cleanly (147/147 tests, unaffected by this correction) and its pane was closed after reading its report; the frontend one was paused mid-flight via Escape and its pane closed before it could add more files under the wrong shape. Cleared to `/ship:build` — dispatch a remediation pass across Tasks 15-20's built output before resuming Task 21. | shape | shape |
| 18 | 2026-08-26T09:15 | shape — technical decision (loop-back from build, before Task 21 dispatch) | `technical — C12: Task 21's a11y scan uses axe-core inside Vitest, not Playwright` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, `plan.md` Task 21 (AC: zero WCAG AA violations) | No a11y scanning tool exists in the frontend dependency tree; Task 22 depends on Task 21 so its Playwright setup can't be borrowed early. Resolved as technical (a dependency-light mechanism inside the existing Vitest+jsdom runner), zero human escalation. Cleared to `/ship:build` — dispatch Task 21. Orchestrator also found, via a real live browser check (`http://localhost:4300`) before dispatch, that `app.html`/`app.ts` still contain the entire unmodified Angular CLI starter splash (logo, "Hello, frontend", doc pills, social links) sitting above `<router-outlet />` — meaning every real screen in the app (login included) renders underneath that placeholder content. Never caught across 20 prior build tasks; folded into Task 21's brief as a required fix (dead content pollutes the accessibility tree on every screen), not a separate task. |
| 17 | 2026-08-26T08:00 | shape — technical decision (loop-back from build, before Task 20 dispatch) | `technical — C11: add GET /api/v1/samples/{sampleId} with origin-or-current-site visibility` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, brief.md R59 | Orchestrator found `SampleTransferController` has only a transfer POST, no way to view a sample at all — Task 20 (cross-site transfer UI) cannot be built without one. Resolved as technical (a direct reading of the already-decided R59 text), zero human escalation. Cleared to `/ship:build` — dispatch the backend fix, then Task 20. |
| 16 | 2026-08-26T07:30 | build — Task 14 gate re-opened again (exception-analyses placeholder found during Task 18 live check) | `n/a — bug fix completing an already-claimed acceptance criterion, no new decision` | Judison Bacalso (agent-executed, orchestrator session + dispatched backend-implementer) | `plan.md` Task 7 (result management: review, locking, unlock) | `GET /api/v1/results/exception-analyses` was a hardcoded `Array.Empty<ResultReviewDto>()` placeholder with a comment admitting as much — Task 7's claimed R44 acceptance criterion ("reviews only exception results") was never actually true. Found only via the orchestrator's own live HTTP check during Task 18's review; no existing test asserted real content. Fixed with a real site-scoped repository query plus a genuine new integration test (a control case: one analysis with an exception, one without, asserting only the former appears). Backend re-verified: 143/143 passing, 0 warnings, live round-trip confirmed against real dev data. |
| 15 | 2026-08-26T06:45 | build — Task 14 gate re-opened again (template rename bug found during Task 17 live check) | `n/a — bug fix, no new decision` | Judison Bacalso (agent-executed, orchestrator session) | `plan.md` Task 4 (original template update) | `AnalysisTemplateService.UpdateAsync` never applied `request.Name` to the entity — a version/tolerance update worked, a rename silently no-opped. Pre-existing since Task 4; the original unit test asserted the new `Version` but never asserted the new `Name`, so it stayed green through a real behavioural gap (same hollow-assertion class as Task 6's calculation-engine bug). Found only via the orchestrator's own live HTTP round-trip during Task 17's review, not by any existing test. Fixed with one line plus the missing assertion added to the existing test. Backend re-verified: 142/142 passing, 0 warnings. |
| 14 | 2026-08-26T05:30 | shape — technical decision (loop-back from build, Task 17) | `technical — C10: R12 work queue served by grouping existing site-scoped Search results by status, no new backend endpoint` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, `plan.md` Task 17 | Task 17's frontend session hit R12 ("Analyst sees work queue split by status") and correctly found no per-analysis assignment concept exists in the data model, routing it back rather than fabricating one — resolved as technical (Search's existing site-scoped, status-bearing results already serve it; group client-side, no new endpoint). Zero human escalation. Cleared to `/ship:build` — resume Task 17's fix-up with the work-queue view now unblocked. |
| 13 | 2026-08-26T04:15 | build — Task 14 gate re-opened (3 backend fixes surfaced during Task 16 frontend integration) | `n/a — infrastructure/contract fixes completing already-decided contracts, no new decisions` | Judison Bacalso (agent-executed, orchestrator session + dispatched backend-implementer sessions) | `shape.md@<pending commit>` §1 contracts; brief R38 | Per `plan.md`'s own §6 risk note ("a UI-driven need for an API change... treat such a change as a normal new backend task, re-opening Task 14's gate"). Three real gaps surfaced only by actually building the frontend against the live API, none caught by any backend test: (1) `GET /api/v1/analyses/{analysisId}` — specified in `shape.md` §1 since Task 3 — was never implemented at all (only readings/exceptions/status POST/PATCH existed); added with a real service method, DTO, and 3 new HTTP round-trip tests (138→141). (2) Every controller action across all 10 controllers returned via anonymous objects or lacked `[ProducesResponseType]`, so the OpenAPI spec never actually documented any response schema — the "generated client matches OpenAPI" contract (`engineering-standards.md` §4) was vacuously true because there was nothing to match. Swept across the whole API (mechanical, no behavior change) — verified every endpoint's spec entry now has a real schema. (3) R38 ("expected range/actual value/reason") — part of Task 3's own reference-vertical acceptance criterion — was never actually surfaced by the API: `ReadingDto.ValidationResult` was a bare status string; the computed `Reason` was silently discarded before the response. Fixed with a structured `ReadingValidationDto`, recomputed correctly on re-fetch using the analysis's *bound* template version (not the current one, per shape.md C7/Task 4 versioning) — 141→142 tests. All three independently re-verified by the orchestrator (real `dotnet build`/`dotnet test`, and live HTTP round-trips against a running instance) before being accepted, not taken from agent self-reports. Backend re-verified: 142/142 passing, 0 warnings. |
| 12 | 2026-08-26T00:30 | shape — technical decision (loop-back from build, before Task 16 dispatch) | `technical — C9: Task 16 additionally builds the shared authenticated layout + login screen` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>`, `plan.md` | Orchestrator surfaced a plan gap before dispatching Task 16 (the reference vertical): no task in Tasks 15-22 builds a login screen, yet the backend's JWT auth (Task 2) and R73 make one a structural precondition for reaching any protected route. Resolved as technical (same class as the backend's C7/C8 loop-back), zero human escalation. Cleared to `/ship:build` — resume Task 16 with the expanded scope. |
| 11 | 2026-08-26T00:00 | build — Task 15 direct fix (orchestrator, before dispatch review closed) | `n/a — infrastructure fix, no new decision` | Judison Bacalso (agent-executed, orchestrator session) | `shape.md@<pending commit>` §1 contracts; `engineering-standards.md@66271df` §4 | Reviewing Task 15's frontend-implementer return found the backend never actually emitted an OpenAPI spec (zero `Microsoft.AspNetCore.OpenApi`/Swashbuckle references in `Program.cs` or the `.csproj`) despite `shape.md`/`engineering-standards.md` §4 already deciding "OpenAPI emitted from the running API" back at Task 3 — a gap the Task 14 gate didn't check for. Same precedent as Task 14's `coverlet.collector` fix (infrastructure for an already-decided contract, applied directly rather than dispatched): added `Microsoft.AspNetCore.OpenApi` 10.0.0 + `builder.Services.AddOpenApi()`/`app.MapOpenApi()`; had to additionally pin `Microsoft.OpenApi` to 2.7.5 (not the transitively-restored 2.0.0/2.0.1) to clear a real `NU1903` high-severity advisory (GHSA-v5pm-xwqc-g5wc, patched at 2.7.5) that `TreatWarningsAsErrors` correctly caught. Re-verified: `dotnet build` 0 warnings/0 errors, `dotnet test` 138/138 still green, and `/openapi/v1.json` confirmed live against a running instance (a real 3.1.1 spec, 32KB, full path/schema tree) — not just asserted from the diff. Task 15's frontend session also self-reported "WHEN `ng lint` runs" as verified by running raw `npx eslint` instead of the actual `ng lint` CLI target, which doesn't exist yet in the scaffolded workspace (`ng lint` fails with "Cannot find lint target") — routed back to the same session as a fix-up, not accepted as-is. |
| 10 | 2026-08-25T22:00 | build — Task 14 backend verification gate | `PASSED` | Judison Bacalso (agent-executed, orchestrator session) | `plan.md@<pending commit>` Tasks 1-13 | All 4 gate criteria independently verified: zero-warning build, CI pipeline checks re-run locally (no real CI infra to run against yet — documented risk, not new), every Tasks 2-13 acceptance criterion (tracked per-task in plan.md §7), 60% coverage floor met at 94.71% (fixed a missing `coverlet.collector` package reference to measure it for real). 138/138 tests passing. Backend cleared for frontend (Tasks 15-22) in a future session per operator request. |
| 9 | 2026-08-25T08:00 | shape — technical decision (loop-back from build, before Task 2 dispatch) | `technical — C7: no separate Role entity; C8: Task 2 owns LimsDbContext's creation, Task 3 extends it` | Judison Bacalso | `shape.md@<pending commit>` | Orchestrator surfaced two plan/shape inconsistencies before dispatching Task 2: (1) plan.md's Task 2 "Role.cs" row conflicted with Task 1's already-built shared-kernel Role enum; (2) plan.md's Task 2→3 dependency only makes sense if Task 2 creates LimsDbContext, not Task 3 as its file list literally reads. Both resolved as technical, zero additions, zero human escalation. Cleared to `/ship:build` — resume Task 2. |
| 1 | 2026-08-25T00:00 | frame — brief approved | `Approve` | Judison Bacalso | `brief.md` (v0.1.0, pre-commit) | Assumption ledger has 0 needs-human rows; question ledger has 2 open out-of-remit rows owned by charter, non-blocking per phases.md |
| 2 | 2026-08-25T01:00 | charter — decisions + constitution confirmed | `Confirm` | Judison Bacalso | `charter.md@a58f960` (base brief), see also interim answers: "Mixed. Backend and Frontend... I will just go directly with claude code." (delivery surface); "It should be separated. Backend will use .NET while frontend will use Angular." (stack split); ".NET 10 will be good" / "Angular 21+ would be better." (versions); "Yes, same SQL Server \"cane-db\"" (persistence); "It will be Entra ID, but for now just do a fallback on user and password." (auth); "Optimistic concurrency, conflict surfaced" (concurrency); "Same as existing LIMS platform" (deployment); "WCAG AA required" (accessibility); "Also i want to use Kendo UI for the components... version should v23" (UI library) | Both question-ledger rows (Q1, Q2) resolved into charter.md §2; DEFINITION phase exit gate now clean (no open rows) |
| 8 | 2026-08-25T07:00 | shape — technical decision (loop-back) | `technical — C6: Angular conventions corrected against a real sibling codebase` | Judison Bacalso | `shape.md@<pending commit>`, `engineering-standards.md`, `plan.md`, `shared-helpers-inventory.md` all updated | Operator provided access to `Databank.WebApp` (the organisation's real Angular 21/Kendo v23 app). Corrected: test runner (Vitest, not Karma/Jest), API layering (`BaseApiService`/`LimsApiService` + `httpResource`/`rxResource`, not a generic client), naming (no `Component` suffix/`.component.` infix, single `lims-` selector prefix), routing (layout-first), environments (4-tier local/dev/uat/prod). Zero additions — still a technical, shape-decidable correction. Still cleared to `/ship:build`. |
| 7 | 2026-08-25T06:00 | shape — technical shape established | `n/a — no human gate; 5 technical decisions, 0 additions` | Judison Bacalso | `shape.md` built from `plan.md@fa5f2fe` + `design-handoff.md@f65e295` + `engineering-standards.md@66271df` | First run. Contracts (rowVersion/409 concurrency, RFC-7807 errors), layering, and 8 shared helpers pre-registered in `ship/shared-helpers-inventory.md`. Zero additions — nothing escalated to the human. Cleared to `/ship:build`. |
| 6 | 2026-08-25T05:00 | design-handoff — written | `n/a — no human gate; handoff written` | Judison Bacalso | `design-handoff.md` built from `audits/codebase-2026-08-25/@3990b17` + `decisions-log.md@d285764` | 12 decided-behaviour rows, 26 unsequenced work items (W1–W26), 12 out-of-scope items (7 not-planned, 4 vNext, 1 revisit-next-release). Audit verdict carried as NOT HANDOFF-READY (greenfield, "gaps are the work" per phases.md) — not silently overridden. Cleared to `/ship:plan`. |
| 5 | 2026-08-25T04:00 | clarify — question resolved | `n/a — 0 clarify-routed rows found; nothing to resolve` | Judison Bacalso | `ship/v0.1.0/audits/codebase-2026-08-25/@3990b17` | Worklist (additions.md + coverage.md clarify-routed rows) was empty — Definition already closed every decision. `decisions-log.md` created with 0 entries, documenting why. Cleared to `/ship:design-handoff`. |
| 4 | 2026-08-25T03:00 | audit — verdict | `NOT HANDOFF-READY (expected — greenfield build inventory)` | Judison Bacalso | `ship/v0.1.0/audits/codebase-2026-08-25/` @ repo commit `79a43d0` | 6 blocker + 20 gap findings, all routed to `plan` (0 to `clarify` — Definition already closed every decision). `coverage.md`'s 26 rows are the build backlog. |
| 3 | 2026-08-25T02:00 | design-request — issued | `n/a — no human gate; request issued` | Judison Bacalso | `design-request.md` built from `brief.md@66271df` + `charter.md@66271df` | Backend (codebase) surface — no external design commissioned; nothing returned. Next: `/ship:audit` against the repo at HEAD (currently empty/greenfield). |
