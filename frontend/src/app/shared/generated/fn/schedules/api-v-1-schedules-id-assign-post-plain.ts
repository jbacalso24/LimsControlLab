/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssignScheduleRequest } from '../../models/assign-schedule-request';
import { ScheduleDto } from '../../models/schedule-dto';

export interface ApiV1SchedulesIdAssignPost$Plain$Params {
  id: number;
      body: AssignScheduleRequest
}

export function apiV1SchedulesIdAssignPost$Plain(http: HttpClient, rootUrl: string, params: ApiV1SchedulesIdAssignPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ScheduleDto>> {
  const rb = new RequestBuilder(rootUrl, apiV1SchedulesIdAssignPost$Plain.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScheduleDto>;
    })
  );
}

apiV1SchedulesIdAssignPost$Plain.PATH = '/api/v1/schedules/{id}/assign';
