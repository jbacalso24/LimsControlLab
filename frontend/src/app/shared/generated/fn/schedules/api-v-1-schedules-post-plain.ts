/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateScheduleRequest } from '../../models/create-schedule-request';
import { ScheduleDto } from '../../models/schedule-dto';

export interface ApiV1SchedulesPost$Plain$Params {
      body: CreateScheduleRequest
}

export function apiV1SchedulesPost$Plain(http: HttpClient, rootUrl: string, params: ApiV1SchedulesPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ScheduleDto>> {
  const rb = new RequestBuilder(rootUrl, apiV1SchedulesPost$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScheduleDto>;
    })
  );
}

apiV1SchedulesPost$Plain.PATH = '/api/v1/schedules';
