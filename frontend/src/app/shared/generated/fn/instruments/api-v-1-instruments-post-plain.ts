/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateInstrumentRequest } from '../../models/create-instrument-request';
import { InstrumentDto } from '../../models/instrument-dto';

export interface ApiV1InstrumentsPost$Plain$Params {
      body: CreateInstrumentRequest
}

export function apiV1InstrumentsPost$Plain(http: HttpClient, rootUrl: string, params: ApiV1InstrumentsPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<InstrumentDto>> {
  const rb = new RequestBuilder(rootUrl, apiV1InstrumentsPost$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InstrumentDto>;
    })
  );
}

apiV1InstrumentsPost$Plain.PATH = '/api/v1/instruments';
